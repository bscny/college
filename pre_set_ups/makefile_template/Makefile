# Compiler and flags
CXX = g++
CXXFLAGS = -Wall -Wextra -O2

# Project root directory (relative to build directory)
PROJECT_ROOT = ..

# Include directories for header files when compiling object files
# In this way, we write #include "src1/file.h" where src1 is a folder sitting in root directory
INCLUDES = -I$(PROJECT_ROOT)

# Source directories
SRC1_DIR = $(PROJECT_ROOT)/src1
SRC2_DIR = $(PROJECT_ROOT)/src2
SRC3_DIR = $(PROJECT_ROOT)/src3

# Tell Make where to find source files
VPATH = $(PROJECT_ROOT) $(SRC1_DIR) $(SRC2_DIR) $(SRC3_DIR)

# Find all .cpp files in the directories
MAIN_SRC = $(PROJECT_ROOT)/main.cpp
CATEGORY1_SRCS = $(wildcard $(SRC1_DIR)/*.cpp)
CATEGORY2_SRCS = $(wildcard $(SRC2_DIR)/*.cpp)
CATEGORY3_SRCS = $(wildcard $(SRC3_DIR)/*.cpp)

# All source files
SRCS = $(MAIN_SRC) $(CATEGORY1_SRCS) $(CATEGORY2_SRCS) $(CATEGORY3_SRCS)

# Object file directory
OBJ_DIR = obj

# Object files (put them in obj directory, flatten the structure)
OBJS = $(addprefix $(OBJ_DIR)/, $(notdir $(SRCS:.cpp=.o)))

# Output executable name
TARGET = main

# Default target
all: $(TARGET)

# Build the executable
$(TARGET): $(OBJ_DIR) $(OBJS)
	$(CXX) $(CXXFLAGS) $(OBJS) -o $(TARGET)

# Create obj directory
$(OBJ_DIR):
	mkdir -p $(OBJ_DIR)

# Universal rule: compile any .cpp to obj/%.o
$(OBJ_DIR)/%.o: %.cpp
	$(CXX) $(CXXFLAGS) $(INCLUDES) -c $< -o $@

# Clean up build files
clean:
	rm -rf $(OBJ_DIR) $(TARGET)

# Rebuild everything
rebuild: clean all

# Mark targets as phony
.PHONY: all clean rebuild
